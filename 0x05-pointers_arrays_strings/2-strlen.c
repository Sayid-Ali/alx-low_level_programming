#include "main.h"



/**
 * _strlen - Returns the length of a string.
 * @str: The string to get the length of.
 * Return: returns The length ofthe string.
 */
int _strlen(char *s)
{
	return (strlen(s));
}

